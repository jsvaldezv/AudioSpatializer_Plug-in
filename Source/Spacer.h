#pragma once

class Spacer
{
public:
    
    Spacer();
    ~Spacer();
    
    void processSpacer1(float* inAudioSpacer, float pTheta, float* outAudioSpacer, int inNumSamplesToRender);
    void processSpacer2(float* inAudioSpacer, float pTheta, float* outAudioSpacer, int inNumSamplesToRender);
    void processSpacer3(float* inAudioSpacer, float pX, float pY, float* outAudioSpacer, int inNumSamplesToRender);
    void processSpacer4(float* inAudioSpacer, float pX, float pY, float* outAudioSpacer, int inNumSamplesToRender);
    void processSpacer5(float* inAudioSpacer, float pX, float pY, float* outAudioSpacer, int inNumSamplesToRender);
    void processSpacer6(float* inAudioSpacer, float pX, float pY, float* outAudioSpacer, int inNumSamplesToRender);
    void processSpacer7(float* inAudioSpacer, float pX, float pY, float* outAudioSpacer, int inNumSamplesToRender);
    void processSpacer8(float* inAudioSpacer, float pX, float pY, float* outAudioSpacer, int inNumSamplesToRender);

private:
    
};
